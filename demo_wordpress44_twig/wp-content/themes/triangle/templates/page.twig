{% extends "base.twig" %}
{% block content %}
    {#<div class="content-wrapper">
            <article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
                    <section class="article-content">
                            <h1 class="article-h1">{{post.title}}</h1>
                            <div class="article-body">
                                    {{post.content}}
                            </div>
                    </section>
            </article>
    </div>#}
    <!-- /content-wrapper -->
    <!-- Main -->
    <section id="main" class="wrapper post-type-{{post.post_type}}">
        <div class="container">
            <header class="major special">
                <h2>{{post.title}}</h2>
            </header>
            {% if post.get_thumbnail %}
                <a href="{{post.permalink}}" class="image fit"><img src="{{post.get_thumbnail.get_url}}" alt="{{post.title}}" /></a>
            {% endif %}
                {{post.content}}
        </div>
    </section>
{% endblock %}